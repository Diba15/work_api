{
    "functions": {
        "api/index": {
            "runtime": "vercel-bun@1.0.0"
        }
    },
    "buildCommand": "bun install && bun run build",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api"
        }
    ]
}